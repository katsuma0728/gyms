<h2>ユーザー編集ページ</h2>

<%= form_with model: @user, url: user_path, method: :patch do |f| %>

  <div class = "field">
    <%= f.file_field :profile_image, accept: "profile_image/*" %>
  </div>

  <div class="field">
    <h5>ユーザーネーム</h5>
    <%= f.text_field :name %>
  </div>

  <div class = "field">
    <h5>年齢</h5>
    <%= f.date_select :birth_date, use_month_numbers: true, start_year: 1930, end_year: (Time.now.year - 10), default: Date.new(1989, 1, 1) %>
  </div>

  <div class = "field">
    <h5>性別</h5>
    <%= f.select :sex , User.sexes_i18n.keys.to_a, { include_blank: '選択してください'} %>
  </div>

  <div class = "field">
    <h5>ひとこと！</h5>
    <%= f.text_area :introduction %>
  </div>

  <div class="field">
    <h5>メールアドレス</h5>
    <%= f.email_field :email %>
  </div>

  <div class="actions">
    <%= f.submit "保存" %>
  </div>
<% end %>

<div class = "log_out">
  <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
</div>

<div class = "withdrawal">
  <%= link_to "退会する", confirm_unsubscribe_path %>
</div>