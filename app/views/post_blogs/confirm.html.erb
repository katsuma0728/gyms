<div class = "container">
  <div class = "row">
    <div class = "col-md-8 offset-md-2 mt-3">
      <h5>下書き一覧</h5>
      <% if @post_blogs.present? %>
        <%= render 'card', post_blogs: @post_blogs %>
      <% else %>
        <p>下書きはありません。</p>
      <% end %>
    </div>
  </div>
</div>